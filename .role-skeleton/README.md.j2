# {{ role_name }}

What this role owns, in a sentence or two.

## Variables

See `defaults/main.yml` (commented) and `meta/argument_specs.yml`.

## Notes

Anything a future maintainer must know that doesn't fit a defaults comment:
external context, manual steps, revert procedure beyond the defaults note.
